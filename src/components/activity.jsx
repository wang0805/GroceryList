import React, { Component } from "react";
import { withRouter } from "react-router-dom";

class Activity extends Component {
  render() {
    return <div>testing activity</div>;
  }
}

export default withRouter(Activity);
